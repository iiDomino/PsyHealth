(async function(){"use strict";const form=document.getElementById("adminLoginForm"),error=document.getElementById("adminLoginError");if(await window.PsyHealthStorage.adminSession()){location.replace("history.html");return;}form.addEventListener("submit",async event=>{event.preventDefault();const button=form.querySelector("button");button.disabled=true;button.textContent="正在登录…";error.textContent="";const data=new FormData(form);try{await window.PsyHealthStorage.adminSignIn(String(data.get("email")).trim(),String(data.get("password")));const role=await window.PsyHealthStorage.myRole();if(role.role==="organization"&&!role.usable)throw new Error(role.status==="pending"?"机构账号正在等待系统管理员开通。":"机构账号已到期或暂停使用。");location.href="history.html";}catch(err){await window.PsyHealthStorage.adminSignOut().catch(()=>{});error.textContent=err.message;button.disabled=false;button.textContent="登录管理中心";}});})();
